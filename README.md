# SkyAtlas
Repositorio para el proyecto de Programacion.

## Setup (Linux / Codespaces)
Ejecuta el script para instalar dependencias del sistema y paquetes npm:

```bash
bash scripts/setup.sh
```

Luego puedes iniciar Expo:

```bash
npx expo start
```
